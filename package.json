{
  "name": "the-story-so-far",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js src/admin.js --bundle --outdir=dist --minify --format=esm"
  },
  "dependencies": {
    "firebase": "^12.6.0",
    "esbuild": "^0.27.0"
  }
}
